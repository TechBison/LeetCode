//#include <bits/stdc++.h> 
#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main()
{
    string s="ab#c",t="ad#c";
    string es = "#";
    //s.erase(remove(s.begin(), s.end(), &es), s.end());
    //t.erase(remove(t.begin(),t.end(),&es),t.end());
    s.
    std::cout << s << std::endl;
    std::cout << t << std::endl;
}